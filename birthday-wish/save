"""
import smtplib
#gmail account
gmail_username = 'pythontester630@gmail.com'
gmail_password = 'tupdglgcdpnhohme'

#yahoo account
yahoo_username = 'amritmann1@yahoo.com'
yahoo_password = 'Raman123'

#create object form smtplib
with smtplib.SMTP('smtp.gmail.com') as connection:
    connection.starttls()
    #login to gmail account
    connection.login(gmail_username, gmail_password)
    #send email
    connection.sendmail(
    from_addr=gmail_username, 
    to_addrs=yahoo_username, 
    msg='Subject: Happy Birthday\n\nHappy Birthday to you'
    )
"""